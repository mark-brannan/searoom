// Types over the colregs data files and the engine's evaluation output,
// re-exported from colregs-engine.

export type {
  Display,
  DisplayLight,
  DisplayEvaluation,
  FactRecord,
  Modality,
} from 'colregs-engine';

export type {
  Arc,
  ApplicabilityData,
  Constraint,
  ConditionalInclude,
  Entry,
  FactValue,
  GeometryData,
  LightDef,
  LightsData,
  LightSpec,
  ModalityBy,
  NumericConstraint,
  Paragraph,
  Predicate,
  RulesData,
} from 'colregs-engine/schema';
